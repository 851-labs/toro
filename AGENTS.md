# Toro Engineering Notes

- Keep source files under 500 lines.
- Preserve the domain boundaries: domain, ACP, environments, host, desktop, UI.
- Prefer the Host API abstraction over direct desktop-only calls in React.
- Every feature should include automated checks and a manual verification note.
